# gitgauge-stage2
Extends GitGauge to score entire developer cohorts. Loads profiles into a pandas DataFrame, computes NumPy percentile rankings, generates two labelled charts, and exports a personalised Markdown report per developer with their score and top fixes.
